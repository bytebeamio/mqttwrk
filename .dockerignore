# These are backup files generated by rustfmt
**/**/*.rs.bk
**/**/metadata
**/**/preprocessed_configs/

# Docker's build context gets alot larger due to target folder so docker doesn't
# work as expected so we need to exclude target folder.
# **/target should include ./target as well but docker doesn't seem to take that into account.
# Should be fixed once cross(tool we use to easily cross compile binaries) start
# using BuildKit for docker
./target
./Cargo.lock

#Added by cargo

**/target
**/tools/provision/provision
**/tools/console/console
**/tools/clients/v4/v4
**/tools/clients/v5/v5

**/.idea
**/out.txt
**/*.pb
**/*.swp
